/**
 * Toggle items in the outline variant barely change on selection, which is the
 * one thing the schedule form has to make obvious. Selected picks up the accent.
 */
export const TOGGLE_ITEM =
  'rounded-lg! border! transition-colors data-[state=on]:border-primary! data-[state=on]:bg-primary/10 data-[state=on]:text-foreground data-[state=on]:font-semibold'
